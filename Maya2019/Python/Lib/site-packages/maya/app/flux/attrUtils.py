import maya.cmds as cmds

def checkNiceNames(namesMap):
    for nType, attrs in namesMap.iteritems():
        for attr in attrs:
            exists = cmds.attributeQuery(attr, typeExact=nType, exists=True)
            if not exists:
                nodeTypes = cmds.nodeType(nType, i=True, itn=True) or []
                print 'Error setting nice name. Attribute %s inherited from either of the following: (%s)' % (attr, ','.join(nodeTypes))
        

def registerNiceNames(namesMap):
    for nodeType, attrs in namesMap.iteritems():
        for attr, niceName in attrs.iteritems():
            attr = cmds.attributeQuery(attr, typ=nodeType, shortName=True)
            key = 'n_%s.a_%s_niceName' % (nodeType, attr)
            cmds.displayString(key, value=niceName, replace=True)

def niceName(nodeType, attribute):
    return cmds.attributeQuery(attribute, typ=nodeType, niceName=True)

def attrType(nodeType, attribute):
    return cmds.attributeQuery(attribute, typ=nodeType, attributeType=True)
# ===========================================================================
# Copyright 2018 Autodesk, Inc. All rights reserved.
#
# Use of this software is subject to the terms of the Autodesk license
# agreement provided at the time of installation or download, or which
# otherwise accompanies this software in either electronic or hard copy form.
# ===========================================================================
